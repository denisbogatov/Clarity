# Clarity

The rules for this tree live in `AGENTS.md`. This file exists only so that Claude Code, whose
project memory file is `CLAUDE.md`, loads them without depending on whether the running version
also reads `AGENTS.md` on its own. Keep the rules in `AGENTS.md`; keep this file one import.

@AGENTS.md
